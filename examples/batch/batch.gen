This file is made to showcase dynamic redirection, and its capacity to create batch outputs.

@run {
    %scale: [1E1] * 3 + [1E3] * 2
    %dataset_count: len(scale)

    @for i in dataset_count {
        @redirect f"batch.{i + 1}.in"
        n: int(scale[i])    \n
        @loop n {
            a: r(1, int(1E8))
            a + r(1, int(1E8))   \n
        }
    }
}